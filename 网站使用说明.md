# 网站使用说明（大白话版）

> 本文件、启动网站.bat、关闭网站.bat 都放在 **`G:\CMakePJ\CompanyWebsite`**（前端）文件夹里。

## 一、先搞清楚两个文件夹

`G:\CMakePJ` 里有两个有用的文件夹，记住这一句就行：

- **`CompanyWebsite`** = 你的**网站本身**（用户看到的页面）→ 叫**前端**（就是本文件所在的文件夹）
- **`CompanyWebsite-Backend`** = 记录"多少人来看、看了哪些页"的程序 → 叫**后端**

> 顺口溜：**名字带 `-Backend` 的是后端，不带的就是前端（网站）。**
> 其他 `Blitzball-Web` 之类的都是没用的旧副本，别管。

---

## 二、怎么启动？

**双击本文件夹里的 `启动网站.bat` 就行。** 完事。

它会自动：① 启动 Docker（没开会自动开，要等 1~2 分钟）→ ② 启动后端+数据库 → ③ 启动前端网站 → ④ 自动打开浏览器。

启动后会弹出一个标题为「前端网站-关掉此窗口=停止网站」的黑窗口，**别关它**（关了网站就停了）。

---

## 三、启动后去哪看？

| 你想看什么 | 打开这个网址 | 账号密码 |
|-----------|-------------|---------|
| **你的网站** | http://localhost:8080 | 不用登录 |
| **访问统计** | http://localhost:8090/dashboard | 账号 `admin`　密码 `change-me-please` |

---

## 四、怎么关闭？

**双击本文件夹里的 `关闭网站.bat`。** 数据不会丢，下次启动还在。

---

## 五、出问题先看这里

**双击 bat 闪一下就没了 / 没反应？**
右键 bat →「以管理员身份运行」试试；或者在文件夹地址栏输入 `cmd` 回车，把 bat 名字拖进去回车，能看到报错。截图发我。

**说"Docker 等了2分钟还没起来"？**
手动打开「Docker Desktop」，等右下角小鲸鱼图标变绿（不转圈），再重新双击 `启动网站.bat`。

**浏览器打开是空白 / 打不开？**
等几秒刷新。还不行就看那个黑窗口有没有报错。

**统计面板让我输账号密码？**
账号 `admin`，密码 `change-me-please`。

---

## 六、上线前必须改的两件事（现在只能本机玩）

1. **前端**：把本文件夹里 `index.html / blitz.html / closecrab.html` 中的
   `http://localhost:8090` 换成后端服务器真实网址。
2. **后端**：在 `CompanyWebsite-Backend` 建 `.env`（照 `.env.example` 抄），
   把 `ALLOWED_ORIGINS` 改成前端真实网址，并改掉默认密码 `DASHBOARD_PASS`。

到时把域名告诉我，我帮你改。

---

## 七、目录速查

```
G:\CMakePJ\
├── CompanyWebsite\          ← 前端（你的网站），启动脚本和本说明都在这里
│   ├── 启动网站.bat          ← 双击启动
│   ├── 关闭网站.bat          ← 双击关闭
│   ├── 网站使用说明.md        ← 本文件
│   ├── index.html / blitz.html / closecrab.html
│   └── assets\ ...
└── CompanyWebsite-Backend\  ← 后端（访问统计）
```
